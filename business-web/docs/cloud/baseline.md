# 仓库基线与证据范围

## 当前神舟包（2026-09-25）

当前共享业务核心已同步完整小程序1.0.9，源修订 `de08496ec861716a4d9ce026534abfee5d0ec099`，259个共享文件、27页、14组件。保留神舟三处定制和菜单移除；周报按周显示、近14天上传资料生成本地规则草稿。Web展示/npm基线0.6.12和依赖未升级，后端、中台、Agent未修改。

本轮构建、134项默认检查、23项隔离浏览器用例通过，正式账号、后端写入和Agent未联调。完整范围以[当前交接](../../修改交接文档.md)和[本轮验收](../../验收/1.0.9同步-20260925/验收记录.md)为准；下文为来源工程的历史记录，不继承其正式接口验收结论。

## 来源工程历史（2026-09-20）

本仓库起点是 Web 0.6.12，小程序业务基线1.0.6，原前端修订 `6b8aa7a8a5e5bed99341551986f3525df54197dc`。原代码ZIP SHA-256：`a7833312b50c4389defcbdf9ce5e2468727a6689b407b34cc43286fb9fb08b9c`。

仓库整理新增合成预览入口、云端初始化、CI、CLAUDE.md和可移植文档，业务版本沿用0.6.12。共享小程序源码、Web业务模块和原页面适配保留。当前在用的代码修订以后以Git提交为准，不把源码中的历史来源修订当作本仓库提交。

该ZIP曾通过41项隔离浏览器检查、1项构建检查、独立解压重建；这是原交付范围的历史证据。首次GitHub上传另外运行当前仓库的构建与单元检查，结果见CI或当前交接记录。后续提交按各次检查结果判断，不继承历史通过结论。

## 已有界面

- 总览：完整侧栏收纳待办和常用操作，右侧业务动态。
- 客户：桌面图表与列表同时显示；总览口径与列表条件各自明确。
- 商机、任务及同类列表：紧凑筛选与列表，详情按内容分栏。
- 认领、任务、商机表单：桌面分区，保留原校验和权限。
- 拜访归档：成功摘要与待办推荐并列；未触发自动获取时可手动获取，采纳仍确认任务。

现行规范与实际页面仍有逐步迁移的差异；模板或CI通过不代表26页全部达标。真实账号、审批写入、Agent效果和跨端同对象读回不在这份基线的完整验收范围内。

## 本地资料和GitHub内容

上传源码、组件依赖、合成测试、当前规范与交接模板；`dist`由云端构建。历史截图、旧报告、本机符号链接、CRM原始数据、账号配置、ZIP不入库。已有本机交付包保留原样。

`VERSION.json` 内的历史文档位置及 `scripts/package_web.py` 指向本机完整交付证据，不属于此精简仓库的默认打包入口。当前仓库使用 `npm run build` 生成页面，用Git提交／PR交接；需要原完整审计ZIP时使用原交付目录，不修改来源哈希来伪造完整证据。
