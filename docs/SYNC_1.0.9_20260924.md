# 神码 1.0.9 同步记录

## 范围与来源

将内部 `be421b12b9dce38aa940e8b546dde1796c3bdef5` 的通用改动移植至神码，包含 1.0.9 功能及 V150、V151 后台性能修复。神码升级前源码为 `a6e9a3c`，在线后端为 `7c7a376` / V125。本次不实施 CRM 对接定制。

保留神码目录、独立 GitHub、AppID `wx2824bdeb58528fd8`、带端口域名、证书、现有公司及账号数据、模型配置和中台绑定。内部仓库只读，无内部生产数据、凭据或发布标识迁入。

## 已同步修改

- 多角色权限、角色模板与单账号增减权限；V126—V151 共 26 份迁移。
- 拜访日期与录音指引、伙伴复用、多负责人独立待办。
- 客户认领筛选及拼音搜索、团队人均排名、地图导航和金额显示修复。
- 日志、飞书同步状态及客户列表的权限查询性能修复。
- Agent 提示词和契约；保留神码事实约束。新增加的内部发布副本不覆盖客户权威提示词。
- 补齐 `pypinyin==0.55.0` 依赖锁、固定部署依赖及已有数据库升级工具。

逐文件来源见 [移植清单](UPSTREAM_SYNC_20260924.json)。原始派生证据继续保留在 SOURCE_MANIFEST.json。

## 本地验证

后端单元 1842 通过（未配置数据库的 729 项跳过，集成单独执行）；前端 831 通过；真实 PostgreSQL 集成 711 通过、20 条件跳过；数据库单元 5 通过、12 历史条件跳过。模型配置、并发任职和客户风险权限专项通过。

神码新装检查通过。V125 → V151 升级演练覆盖两家合成公司、26 个账号：原账号、密码、任职、客户和归属记录逐行一致；销售自身范围及跨公司隔离正常；运行角色不绕过 RLS、无密码表及改授权权限；二次迁移全部 unchanged。另完成 TLS 部署检查及 12 项 Agent 提示词生成检查。

证据：[本地验证摘要](evidence/sync-1.0.9-20260924/local-validation.json)。测试库均为本机临时库，执行后删除。

## 运行交付

2026-09-24 已完成：

| 项目 | 结果 |
| --- | --- |
| 代码 | PR #19 合并；完整 CI 通过。业务运行源码为 `bb02f639da4dbdd0d5a8f704d61ae7d234a4e816`，合并提交 `362bd31` 与其源码树一致；后续提交为交付记录及验收工具更新 |
| 销售服务器 | API、Worker 均运行上述提交，公网版本与就绪检查正常 |
| 数据库 | 实际 V151，119 份迁移校验；二次迁移全部 unchanged；运行角色无 SUPERUSER/BYPASSRLS |
| 备份恢复 | `/var/backups/shenma-sales/20260924T115926Z-v151`；实际恢复至临时库，103 张表行数一致，临时库已删除 |
| 数据保全 | 账号、密码、任职、客户及归属记录在迁移前后逐行摘要一致 |
| 账号 | 13 个既有测试账号登录与相应功能通过；跨公司访问拒绝；未新建客户、商机或拜访记录 |
| 浏览器 | 客户管理及新版权限管理页面已实看，7 类角色模板正常显示 |
| 中台 | 保留 12 个 Agent，更新其中 6 个提示词；模型、工具、Agent 标识及试点范围不变；两家公司共 4 处已绑定能力的快照记录配套更新 |
| Worker 联调 | 客户建议、商机判断、拜访结构化、拜访质检 4 项通过，无兜底；约 1.96—5.43 秒，属于连接与执行验证 |
| 契约用例 | 更新后的 8 个合成用例全部通过，约 1.68—2.71 秒；未创建正式业务记录，不等同于客户真实业务全流程验收 |
| 小程序 | AppID `wx2824bdeb58528fd8` 编译预览成功，包大小 1,575,131 字节；尚未完成本版手机端完整验收，未提交微信正式审核 |

CI：[PR19 验证](https://github.com/Userwei0418/SalesBuddy-Shenma/actions/runs/35995105286)。现场证据：[运行验证](evidence/sync-1.0.9-20260924/runtime-validation.json)、[Agent 发布](evidence/sync-1.0.9-20260924/agent-publications.json)、[小程序预览](evidence/sync-1.0.9-20260924/mini-program-preview.json)。

## 使用及验证边界

- 新版管理员、运营模板只提供后台入口；`OPSAdmin`、`YY001/YY002` 用于管理后台。销售、主管、经理、FDE 等业务账号用于小程序，账号密码未改变。
- 4 项核心 Agent 继续保持原有试点范围。测试公司的试点到期时间为 2026-09-26 14:48 左右（北京时间），精确值见运行回执；账号本身没有随之到期。经营问数保持关闭。
- 中台当前协议不能证明一次执行实际使用的发布快照，回执保持 `runtime_snapshot_verified=false`，不把预期快照编号当成运行证明。
- 离线安装预检发现缺少构建依赖 `editables`，补齐 0.5 wheel 后成功；当时尚未停服或迁移。完整离线依赖包包含运行依赖与构建依赖，不仅是 requirements 列表。
- 原验收工具硬编码旧证书路径，首次直接契约测试失败；工具已改为读取客户运行配置，默认使用系统可信 CA，没有关闭 TLS 校验。修正后 8 项通过，历史失败回执保留。
- 内部仓库结束时仍为干净的 `be421b12b9dce38aa940e8b546dde1796c3bdef5`，本轮未修改、推送或部署内部版本。
