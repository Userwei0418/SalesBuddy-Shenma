# 问数、风险与待办接入验收

2026-09-24 在神码销售服务器使用实际服务账号 UID 998，通过保留域名校验的中台客户端执行六组合成调用。未读取或写入 CRM 业务记录，也未扩大销售侧试点范围。

| 能力 | 输入与结果 | 当前结论 |
|---|---|---|
| 经营问数 | 已有任务统计 3/7 正确；没有回款数据时明确缺失，未编造金额 | 两个样例通过，尚待业务入口端到端验收 |
| 个人风险 | 识别预算未批准，空资料不生成风险；日期没有时分时返回 null | 时间问题修正；仍把“待审批”扩写为“审批停滞”，语义验收未通过 |
| 今日待办 | 保留来源 9 月 22 日期限，没有按当前时间改期；空资料返回空列表 | 两个样例通过，尚待实际生成、去重与展示链路验收 |

首轮问数输出出现内部字段名，风险输出将日期补成无依据的 08:00。已更新客户中台两项提示词并发布、回读验证：问数只使用业务名称；风险缺少具体时分时不生成精确截止时刻。复测六组结构与显式断言通过，不等于全部业务语义通过。

风险默认提醒仍由现有后端 `future_due()` 处理；null 不表示已完成来源期限的完整业务处理。风险输出还存在无依据的“停滞”和内部字段前缀，继续保留未启用状态。

证据：

- `evidence/workbench-agent-business-20260924-v1.json`：首次完整合成输出。
- `evidence/workbench-agent-business-20260924-v2.json`：提示词修正后完整合成输出。
- `evidence/shenma-agent-catalog-publications.json`：两项新快照及提示词哈希。

复现：在销售服务器以 root 运行已维护的 `deployment/verify-agent-business.py --suite workbench --output <新的受限结果路径>`，使用当前部署后端的 Python 环境与模块路径。脚本会切换至实际服务账号，只发送合成资料；每次使用新输出文件，避免覆盖失败证据。

本地相关契约与平台路由测试 69 项通过。作战地图和经营报告不在本轮六组样例覆盖范围内。
