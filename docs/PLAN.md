# 神码独立部署计划

## 目标

建立独立本地目录与私有 GitHub，部署销售系统和中台，完成客户环境下的业务链路。商汤代码、远程仓库、数据库、小程序与发布流程不受影响。

## 步骤与验收

1. **项目隔离**：固定销售 1.0.8 / V125 和中台 20260923 来源；只保留源码、系统配置及合成测试，不复制真实环境证据/绑定；新仓库无商汤推送目标。
2. **环境准备**：再次核对两台空白系统、盘空间、网络、依赖源；安装到独立路径，保留失败日志，不覆盖已有业务。
3. **中台安装**：Docker Compose 17 服务、空实例、HTTPS 本机验证、独立管理员；镜像下载/服务就绪与模型验收分别记录。
4. **销售安装**：Python 3.12+、本机 PostgreSQL、V125 迁移、最小权限、初始公司和管理员、API/Worker；受限真实账号验证读写及拒绝越权。
5. **域名与联调**：使用用户确认的销售 28899、中台 18899 入口，通过 DNS-01 取得可信证书；新中台发布销售 Agent，客户模型配置后验证真实调用和业务输出。
6. **小程序**：收到客户 AppID 后配置，合法域名、真机登录/录音/上传与业务链路验收；未经独立授权不提交正式审核。
7. **交付**：源码/镜像/迁移/进程版本、备份恢复、部署说明、独立定开工作流完整登记。

## 当前状态

- 2026-09-23：已创建目标模式；来源 main 3e5b904，源工作区干净。新目录源码抽取完成；中台外层及 2,282 文件校验通过。
- 两台 SSH 和 sudo 可用；客户机器为 Ubuntu 22.04、16 逻辑核、约 31GiB 内存，根分区约 58GB。销售系统与中台按下表独立部署。
- 独立私有仓库 `Userwei0418/SalesBuddy-Shenma` 已创建；中台原始发行包已上传私有 Release。
- GitHub CI 已通过：前端 773 项、后端单元 1,796 项、迁移静态 5 项、PostgreSQL 16 集成 609 项；另通过客户初始化、运行权限和服务账号密钥读取检查。数据库阶段有 20 项跳过，未计为通过。证据：https://github.com/Userwei0418/SalesBuddy-Shenma/actions/runs/35838588961。
- 中台 12 个基础镜像已全部传入客户服务器并通过压缩包 SHA256 校验，离线安装完成。
- 销售系统已安装：PostgreSQL 16.15 / V125、API、Worker、管理后台；实际受限角色、初始管理员登录/退出、强制首次改密和服务账号密钥读取验证通过。客户、商机、拜访均为 0 条；未导入商汤业务数据。
- 销售本机及公网 28899 已通过指定临时证书的 HTTPS 验证；系统默认信任仍不通过，正式可信证书尚待配置。用户提供 SenseAudio 调试配置后，应用 readiness 已返回 ok。
- 已核实预留端口 NAT 为销售 28899、中台 18899；销售 28899 的外部 HTTPS 和版本接口已通过指定公共证书的验证。域名网关配置不能沿用职责互换前的对应关系。
- 中台 Docker 29.1.3、Compose 2.40.3、Buildx 0.30.1 已安装。17 项 Compose 服务检查通过（16 个运行，权限初始化任务正常退出）；公网 18899 API、登录页与品牌资源通过指定临时证书的 HTTPS 验证。独立管理员登录/账户信息/退出通过；1 个工作空间、0 个应用。销售与中台双向内网 HTTPS 连通。模型与销售 Agent 真实业务调用尚未接入。
- 两台已安装 Certbot，并启用每日两次的系统自动续期任务。HTTP-01 测试因公网 80 超时失败后，用户确认保留带端口地址并切换 DNS-01。权威 DNS 为阿里云/万网，正式签发与续期演练待对方运维配置，详情见 TLS.md。
- 模型：用户已提供 SenseAudio 调试 Key，客户服务器模型列表及真实合成请求通过；销售后端实际 SenseAudioClient 以运行用户调用通过，readiness 为 ok。中台模型供应商配置与 Agent 接入继续推进。
- 等待：客户 AppID、对方运维完成可信证书配置。
- 完整中台 Web 开发源码尚未提供；已有运行构建产物可用于本次安装。

## 分工

| 主机 | 账号 / SSH端口 | 内容 |
|---|---|---|
| 172.22.9.234 | salesbuddy / 32222 | API、管理端、Worker、销售 PostgreSQL、文件 |
| 172.22.9.233 | opsbuddy / 12222 | Agent 中台及其自身存储服务 |
