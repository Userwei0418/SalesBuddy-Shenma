# 神码小程序协作入口

先读仓库根 AGENTS.md 和 docs/DEPLOYMENT.md，核对当前目录、Git remote、分支及版本。仅操作神码独立项目。API 指向客户域名，AppID 使用客户提供值。任何商汤账号、密钥、飞书绑定和生产数据均不得引入。客户数据库中写入即为真实写入，验收数据必须单独标识并按流程清理。

先完成本地测试和客户环境验证，再打包交付。微信上传与正式审核发布分开授权。
